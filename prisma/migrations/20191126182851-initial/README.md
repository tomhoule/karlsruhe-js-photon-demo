# Migration `20191126182851-initial`

This migration has been generated by Tom Houlé at 11/26/2019, 6:28:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Dog" (
  "email" TEXT    ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  
);

CREATE TABLE "lift"."_DogToUser" (
  "A" INTEGER    REFERENCES "Dog"(id) ON DELETE CASCADE,
  "B" TEXT    REFERENCES "User"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."User.id" ON "User"("id")

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."_DogToUser_AB_unique" ON "_DogToUser"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191126182851-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,22 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id    String  @default(cuid()) @id @unique
+  email String  @unique
+  name  String?
+  likes Dog[]
+}
+
+model Dog {
+  id      Int     @id
+  name    String
+  email   String?
+  likedBy User[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191126182851-initial)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126182851-initial'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
