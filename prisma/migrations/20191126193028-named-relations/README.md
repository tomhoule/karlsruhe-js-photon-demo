# Migration `20191126193028-named-relations`

This migration has been generated by Tom Houlé at 11/26/2019, 7:30:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "lift"."_DogToUser_AB_unique"

DROP TABLE "lift"."_DogToUser";

CREATE TABLE "lift"."_likes" (
  "A" INTEGER    REFERENCES "Dog"(id) ON DELETE CASCADE,
  "B" TEXT    REFERENCES "User"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."_likes_AB_unique" ON "_likes"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191126183942-remove-unique..20191126193028-named-relations
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
@@ -10,13 +10,13 @@
 model User {
   id    String  @default(cuid()) @id @unique
   email String
   name  String?
-  likes Dog[]
+  likes Dog[]   @relation("likes")
 }
 model Dog {
   id      Int     @id
   name    String
   email   String?
-  likedBy User[]
+  likedBy User[]  @relation("likes")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191126193028-named-relations)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126193028-named-relations'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
