# Migration `20191126193139-reunique`

This migration has been generated by Tom Houlé at 11/26/2019, 7:31:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191126193028-named-relations..20191126193139-reunique
--- datamodel.dml
+++ datamodel.dml
@@ -1,16 +1,16 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
 }
 model User {
   id    String  @default(cuid()) @id @unique
-  email String
+  email String  @unique
   name  String?
   likes Dog[]   @relation("likes")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191126193139-reunique)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126193139-reunique'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
