# Migration `20191126183942-remove-unique`

This migration has been generated by Tom Houlé at 11/26/2019, 6:39:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "lift"."User.email"
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191126182851-initial..20191126183942-remove-unique
--- datamodel.dml
+++ datamodel.dml
@@ -1,16 +1,16 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
 }
 model User {
   id    String  @default(cuid()) @id @unique
-  email String  @unique
+  email String
   name  String?
   likes Dog[]
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191126183942-remove-unique)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126183942-remove-unique'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
